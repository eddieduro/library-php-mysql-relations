<!DOCTYPE html>
<html>
    {% include 'header.html' %}
    <body>
        <div class="container">
            <h2>Librarian</h2>

        <section class="book_list">

                {% include 'navbar.html' %}
                {% if books is not empty %}
                <h4>List of books in the library: </h4>
                <ul>
                {% for book in books|sort %}
                    <li><h5> Title: <a href="/book/{{ book.getId }}">{{ book.getTitle }}</a></h5></li>
                    <form action="/delete/book/{{ book.getId }}" method="post">
                        <input name="_method" type="hidden" value="delete">
                        <button type="submit" class="btn">Delete</button>
                    </form>
                {% endfor %}
                </ul>
                {% endif %}
                <form  class="col s8" action="/add_book" method="post">
                    <label for="title">Title:</label>
                    <input name="title" id="title">
                    <label for="genre">Genre:</label>
                    <input name="genre" id="genre">
                    <button type="submit" class="btn">Add!</button>
                </form>
            </div>
        </section>
    {% include 'footer.html' %}
    </body>
</html>
